spring:
    application:
        name: AuthorService
    cloud:
        config:
            name: AuthorService
            discovery:
                service-id: ConfigService
                enabled: true
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
