spring:
    application:
        name: ConfigService
    cloud:
        config:
            server:
                native:
                    search-locations: classpath:/shared
    profiles:
        active: native
server:
    port: 9001
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:9002/eureka/
