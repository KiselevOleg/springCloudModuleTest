spring:
    application:
        name: ConfigService
    cloud:
        config:
            server:
                server:
                    git:
                        uri: classpath:/shared
server:
    port: 9001
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:9002/eureka/
