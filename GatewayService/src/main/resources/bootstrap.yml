spring:
    application:
        name: GatewayService
    cloud:
        config:
            name: GatewayService
            discovery:
                service-id: ConfigService
                enabled: true
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
